{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "preinstall": "node ./scripts/preinstall.mjs",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "build:vercel": "corepack pnpm --filter @workspace/api-server build && corepack pnpm --filter @workspace/crown-blade build",
    "db:push": "corepack pnpm --filter @workspace/db push",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "pnpm@10.17.1",
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
